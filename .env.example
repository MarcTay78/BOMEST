# Leave both unset to run against in-memory mock data (see src/data/mockStore.ts).
# Set both to switch the app to a real Supabase project (see supabase/migrations/0001_init.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
